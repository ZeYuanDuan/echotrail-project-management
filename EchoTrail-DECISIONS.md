# EchoTrail 決策紀錄與待確認事項

本文件記錄 [EchoTrail-MVP-SPEC.md](EchoTrail-MVP-SPEC.md) 各項規格的決策脈絡（誰在什麼時候決定了什麼、為什麼、考慮過哪些其他方案）與目前尚未拍板的開放問題。SPEC 文件本身只放目前已定案的規格結果，不放討論過程；本文件負責保留討論過程，並追蹤還沒決定的事。

兩份文件的關係：SPEC 是「現在是什麼」，本文件是「怎麼決定成這樣的、還有什麼沒決定」。SPEC 章節（例如 A-4、B-2、C-7）與本文件的條目可以互相對照。

〈一、決策紀錄〉每個日期標題結尾標註改動者 `（姓名）`；同一天若有多人分別補充，改在該補充段落前另外標註，方便協作時知道找誰確認。

---

## 一、決策紀錄（依時間序）

### 260826　專案起點

- 依據羽完成的《EchoTrail Prototype 完整腳本規格提示詞》（原本是寫給 AI 直接生成 HTML 原型用的逐字提示詞），整理成正式規格文件格式。
- 發現原本 MVP-SPEC 的 Part 2（不做）清單裡明列「Persona自我畫像卡」不在本輪範圍，但腳本把它當核心功能——決議把 Persona 摘要卡與三項職涯測評（職涯錨點雷達圖／RIASEC／DISC）從 Part 2 移入 Part 1（對應 SPEC 現行 C-2、C-3、C-7、C-8、C-9）。

### 260827　A 組重整為 A-1～A-5

- A組（對話與輸入）從舊版 A-1～A-6 六個功能，改為 5 個入口／流程導向的功能：三種冷啟動入口（A-1文本／A-2履歷上傳／A-3快速匯入）並列、A-4多輪對話、A-5時間軸與儀表板跳轉。
- 原本獨立的「事件時間判斷與確認」「專案／階段標籤」不再是獨立編號，併入 A-4「必要／非必要資料欄位」的具體內容。
- 原 A-6「確認式對話」（逐一針對候選落差點附證據要求使用者確認）判定其運作對象是 B 組比對引擎產出的候選落差點，性質上屬於分析結果確認、不是原始資料輸入，移入 B 組（此時編號為 B-3，後續 260828 B 組再重整後，此功能被完全移除，見下）。

**三種冷啟動入口的優先序**：A-1（文本）、A-2（履歷上傳）是主要入口，A-3（快速匯入）是便利性入口，服務手上已有現成舊資料的使用者。A-2 通常只需操作一次，因為履歷資料不常變動。三者互相獨立，使用者任選一種開始即可，不需要三種都做過一輪。A-4 多輪對話本身即可獨立建立事件進 My Trail，不需要事先透過 A-1／A-2／A-3 任一入口才能使用。

**三種冷啟動入口的介面呈現方式**：使用者提供參考截圖（類似 Gemini 輸入框介面），確認為——主畫面是一個文字輸入框（對應 A-1，跟 A-4 多輪對話共用同一個介面），輸入框正下方直接列出快捷選項清單（不需要先點「＋」才看到），A-2、A-3 各是清單裡的一個選項。

**A-4 多輪對話四項核心規則討論**（原本是開放問題，經團隊逐項討論拍板）：
1. 必要欄位門檻：有值就過關，不判斷內容品質，品質交給 B-2 的信心分數／匹配分數機制把關。
2. 總輪數：軟上限（文案管理預期）＋硬上限（防止無限問下去）混合，實際結束時機看必要欄位是否收集完成，不是輪數到就砍。具體軟／硬上限輪數（例如3輪／8輪）待實測校準。
3. 提前結束時機：達標跳出 CTA 按鈕，但不強制結束對話，使用者可以無視 CTA 繼續聊。
4. 反問與引導邏輯：規則決定「問什麼」（依序檢查必要欄位清單，缺哪個問哪個），語氣與問法交給 LLM 生成。
- 同時定案：批次模式（收集多筆事件）與單一事件深挖模式，不是兩套平行機制——深挖是使用者對批次模式已收斂出的某張事件卡不滿足時，主動點擊才觸發的延伸動作，兩者共用同一套 A-4 規則。

**A-5 定案為 CTA 按鈕本身**：完整流程 A-1/A-2/A-3 → A-4 → A-5，A-5 是 A-4 達標後跳出的 CTA，按下去觸發「分析→產卡→確認→跳轉 My Trail」四步驟序列，明確跳轉終點是 B-1 時間軸（My Trail），不是 C 儀表板。同時定案：批次模式每則事件各自觸發此序列（不是整批一次分析）；分析階段每次都嘗試計算匹配分數（即使第一次使用沒有歷史資料也嘗試，找不到落差點才略過）；Echo Card 修改路徑為「跳轉對話重新產出」＋「直接編輯」兩者並存；使用者中途離開（A-4 未達標）時不允許查看不完整的 My Trail。

**其他 A 組定案**：
- A-1 事件邊界判斷：LLM 依語意分組判斷，判斷完須反問使用者確認是單一事件還是多個事件。
- A-1／A-3 UI 分界：拆成兩個獨立入口，不合併成同一個輸入框。
- A-2 履歷解析：做自動解析（非純手動表單），但技術可行性待與工程 team 確認。
- A-2 更新履歷：沿用同一表單，不做獨立的「更新履歷」入口。
- A-4 時間欄位圖示：先只用文字區分（例如「現在（未確認時間）」），不做額外圖示設計。
- A-4 三段式回應結構技術實作：單一 prompt template，不拆多步驟呼叫。

### 260828　B 組、C 組重整

**B 組重整為 B-1～B-6**：使用者提出新結構——B-1時間軸呈現、B-2事件洞察卡片、B-3跳轉對話按鈕、B-4整體洞察、B-5篩選器、B-6情緒曲線。
- 原 B-3「確認式對話」**正式移除**，被新 B-3「跳轉對話按鈕」取代——不滿意的處理方式簡化成「跳回A-4對話補充」或「直接編輯卡片」兩種，不再需要一套獨立的多輪 confirm 流程。
- 原 B-2「與洞察的連結」（錨點機制）不再是獨立編號，內容併入新 B-1 的資料結構定義。
- 新增 B-5 篩選器、B-6 情緒曲線兩個功能。**B-6 原列於 Part 2「本輪不做」，此次討論後正式移入 Part 1。**
- B-2 資料結構定案：Echo Card 六欄位取代原本的「洞察文字／客觀證據／主觀證據」三欄位，不再並行維護兩套結構。
- B-2 匹配分數計算邏輯定案：LLM 自評（單一 prompt 直接輸出 0-100 分＋文字判讀＋符合/落差關鍵字對照），不接 embedding 或規則型比對。
- B-3 定案：重新產出後直接覆蓋原卡片，不保留版本紀錄；直接編輯過的內容算合法逐字引用來源，不特別標示。
- B-4 定案：篩選範圍改變後不即時重新計算，使用者需手動點擊〈重新分析〉才觸發，控制 LLM 呼叫成本。
- B-5 定案：搜尋列**只搜卡片標題**，不搜全文或情緒用字，類似對話工具搜尋 Session 標題的體驗（使用者明確指定）。同時發現 B-1／B-2 資料結構目前沒有獨立的「標題」欄位，列為開放問題（見下方開放事項）。
- B-6 定案：事件資料結構新增數值型情緒分數欄位（0-100，LLM 評分），與既有三分類欄位並存，兩者不互相取代；折線圖只畫有事件的點、直接連線，不做插值；套用 B-5 篩選器。
- B-1「點擊洞察引用文字時節點呈現方式」定案：捲動置中。「最少事件數門檻」定案：不設門檻，1筆事件即正常顯示。

**C 組重整為 C-1～C-10**：使用者提出新結構——C-1版面配置、C-2 Persona卡片、C-3驅動卡片、C-4文字雲、C-5行為模式排行、C-6軟實力/硬實力證據、C-7職涯錨點雷達圖、C-8 RIASEC長條圖、C-9 DISC象限圖。
- 原 C-1「能力傾向儀表板」（規則型長條圖）**確認捨棄**，不再做長條圖這個呈現形式；其統計邏輯（客觀詞彙頻率統計）併入新 C-4 文字雲。
- 新增 C-1 版面配置，定義 Dashboard 整頁的排版骨架（Persona區塊「左1右3」Grid排版、由上到下的分區順序）。
- 原 C-5～C-9 依序前移為 C-2～C-6，原 C-7、C-8、C-9（三項職涯測評）與 C-10（增量更新規則）編號不變。
- C-1 響應式設計：本輪不特別設計手機／窄螢幕版面，移入 Part 2。
- C-3 三個維度（能力/動機/價值）判斷標準定案：固定寫進 LLM prompt 明確定義三個角度。
- C-4 定案：不做進一步分類（溝通類/專案管理類等），先用原始頻率排行；資料量太少（少於3則事件）設門檻顯示空狀態。**後續（260829）再擴大範圍為「情緒詞＋能力詞」兩類並存，見下。**
- C-5 行為模式最低事件數門檻定案：至少2個事件。
- C-6 與 C-4 的關係：使用者指出軟硬實力偏能力面向，跟 C-4 的能力詞那一半資料重疊，但需要額外一層 AI 歸納把零散證據寫成完整句子，不只是關鍵字列表。
- C-7／C-8／C-9 分數來源：**team 內部有分歧，未解決**——夥伴主張 LLM 直接從對話推論；Webber 主張需要標準化量表填答分數才可信。三項測評是否共用同一套技術方案也待討論。此分歧同時卡住 C-10「哪些分數允許微調」的定案。

### 260829　全文一致性檢查 ＋ C-4 範圍擴大 ＋ A-2 必填矛盾修正

**全文掃描發現的問題與修正**：
- A-2「唯一必填」與後續「三入口任選一種開始」「A-4完全獨立可用」的原則衝突——確認修正為**不必填**，改成 UI 強烈引導，但不阻擋使用者完全只靠 A-4 對話使用產品；若使用者完全沒有客觀資料，B-2 匹配分數、B-4 落差分析顯示對應空狀態，不假裝比對出結果。
- A-5 內文與待確認彙總裡「編輯內容算不算逐字引用」仍標示為待確認，但這題已在 B-2/B-3 定案——同步更新。
- B-2 六格欄位命名不一致（「匹配分數機制」vs「和過去的我是否匹配」）——統一為後者。
- B-4 資料結構仍停留在 B-2 改版前的舊三欄位寫法，沒有跟著同步——已更新為引用 B-2 六欄位。
- Scope 裡 C-4 說明沒跟上「情緒詞+能力詞」的範圍擴大——已同步。

**待釐清但未修正、留給 team 討論的觀察**：
- Part 2 排除清單裡的「逐事件洞察／故事草稿區」跟 B-2「事件洞察卡片」名字很像，容易誤讀成自相矛盾——建議 team 確認 Part2 排除的是不是指另一個東西（例如比較長的自由書寫故事草稿），並在文件補一句區別說明。
- B-1 資料結構「情緒分類」（3類）與「情緒分數」（0-100數值）兩個欄位並存——技術上分類可以直接用分數區間算出來，不一定要各自存一份，是否要精簡成只存分數、分類即時運算，待 team 評估。

**C-4 範圍擴大（同日追加討論）**：文字雲確認改為「情緒詞＋能力詞」兩類並存——一半取自主觀語料的情緒用字（例如「比較焦慮」「自我懷疑」），一半取自客觀詞彙的能力用字（例如「簡報」「企劃」），兩類分開統計頻率、UI上分區呈現。同時確認 C-6 軟硬實力證據跟 C-4 能力詞那一半資料重疊，但需要額外 AI 歸納，不是同一份資料的不同呈現。

**SPEC / DECISIONS 文件拆分**：使用者要求把 SPEC 裡的決策過程、日期標記、待確認清單搬到獨立文件，讓 SPEC 只留乾淨的規格結果。本文件即為拆分後的決策與待確認紀錄。

**Background & Goal 依現行 SPEC 反向調整**：SPEC 從最初只涵蓋 A/B（主客觀比對）擴增到完整的 C 組（Persona 摘要卡、驅動卡片、三項職涯測評、情緒曲線）後，原始 Background 只有兩個痛點，C 組的存在理由被寫成「非Background原始痛點的延伸」——即規格已經跑到背景故事框不住的地方。經使用者要求，反向調整 Background/Goal 對齊現行規格：
- 新增**痛點3**：「累積的事件缺乏歸納，長期下來看不出自己是什麼樣的人」，直接對應 C 組存在的理由，取代原本「非Background原始痛點的延伸」的說法。
- Goal 從單一驗證目標（比對流程的洞察品質）擴增為**兩個**驗證目標：(1) 比對流程洞察品質（對應痛點1、2），(2) 持續累積事件能否組成一份使用者認同的自我畫像（對應痛點3）。
- 「三個核心功能與各自解決的問題」表格 C 列的「對應」欄，從「本輪新排入Part 1，非Background原始痛點的延伸」改成「Background痛點3」。

### 260830　Pitch Deck 製作，新增第三種persona類型

- 製作 pitch deck 草稿（[EchoTrail-Pitch-Deck-草稿.md](EchoTrail-Pitch-Deck-草稿.md)）過程中，使用者針對 Slide 2（服務誰）提出一張三型 Venn 圖：倦怠焦慮者（有情緒×沒行動）、經驗零散者（有行動×沒整理，對應羽絨）、分類卡關者（有整理×沒歸納，對應Webber）。三型收斂在同一個問題「我想要什麼？」。
- 「倦怠焦慮者」是 SPEC Background 原本沒有的第三種類型（原本只有羽絨、Webber兩位persona）。經確認後**同步補進 Background**，讓 SPEC 跟 pitch deck 用語一致。
- 同時記錄一個誠實的範圍提醒（留給簡報講稿用，非SPEC變更）：EchoTrail 現有功能（A/B/C）本質是自我覺察工具，不是行動規劃工具，「倦怠焦慮者」缺的「行動」這塊，EchoTrail唯一沾邊的是C-7職涯錨點的「下一步職涯指引」文字建議，份量很輕——上台被問到時要誠實回答「先幫你想清楚要什麼，行動是下一步」，不要順著三型敘事過度承諾產品範圍。

### 260830　A組插入語音輸入、A-6跳轉目標改為Dashboard（改回兩按鈕序列）、B-3/B-4順序對調、新增側邊欄定義（Webber）

- **A組新增語音輸入，整組重新編號**：使用者要求在 A-3、A-4（多輪對話）中間插入一個語音輸入功能。原 A-4多輪對話 → **A-5**，原 A-5時間軸／儀表板跳轉 → **A-6**，新功能定為 **A-4 語音輸入**：A-1文本冷啟動、A-5多輪對話的輸入框旁提供麥克風圖示，語音轉出的文字視同使用者自己打字，沿用原本入口的解析規則，不重新定義。技術方案（Web Speech API vs. 第三方STT、中文準確度）待評估，見下方開放事項。
- **A-6跳轉終點改為Dashboard**：使用者先提出「A-5（今A-6）跳轉的是儀錶板」，推翻 260827 的原定案（跳轉終點是 B-1時間軸／My Trail）。
- **A-6流程修正為兩個各自獨立的按鈕**：使用者提供 Figma 截圖佐證，確認流程不是「按一次CTA就跳轉」，而是〈產生洞察〉（產出Echo Card供確認）→ 使用者確認 →〈是，請更新到Dashboard〉（正式寫入B-1時間軸＋跳轉Dashboard）兩個按鈕依序觸發。SPEC A-6 已依此改寫，並新增「與C儀表板的關係」段落說明：事件仍會寫入B-1時間軸，只是導向畫面改成Dashboard。
- **新發現：點數／額度機制**：同一張 Figma 截圖顯示第二個按鈕文案為「點擊更新即可消耗點數」，代表更新到Dashboard的動作可能消耗點數／額度，但SPEC完全沒有定義這套機制的規則。已在 A-6、Figma/Wireframe 段落如實記錄畫面上出現的文案，**不自行假設規則**，列為開放問題（見下）。
- **B-3／B-4順序對調**：使用者要求把原 B-3跳轉對話按鈕、B-4整體洞察 兩節順序互換。新順序為 **B-3整體洞察（跨事件模式）**、**B-4跳轉對話按鈕**。全文所有交叉引用（Scope、B-2、B-5、B-6、C-4等處提及的B-3/B-4）已同步更新。
- **新增側邊欄（全域導覽）定義**：使用者提供截圖，補充產品左側固定導覽列的三個入口定義——〈+New〉開新的艾可對話、〈My Trail〉以類似對話工具Chat History的介面呈現B-1時間軸資料、〈My Dashboard〉對應C組儀表板。已寫入 SPEC「Function Detail」開頭、A組之前，不歸屬於A/B/C任一群組。**Scope 清單本次先不補這一段的條目**（使用者要求先留空，待後續確認要不要納入Part 1/Part 2）。
- **Figma / Wireframe 段落補充連結**：Figma流程圖（曼陀號 PM x ENG board）、可互動 Prototype（Netlify）、Pitch Deck（Gamma）三個連結已補進 SPEC 的 Figma/Wireframe 清單。
- **A-6 更名**：功能名稱由「時間軸、儀表板跳轉」改為「**產生洞察按鈕**」，更貼近實際UI（使用者看到的是一個按鈕，不是「跳轉」這個抽象流程名稱）。SPEC 內 Scope、A-3、A-6 標題三處已同步更名；DECISIONS.md 260830 前段條目提及的舊名「時間軸／儀表板跳轉」為當時討論脈絡的原文，保留不改。
- **C-3 更名**：「驅動卡片」加註副標「（共鳴之錨）」，成為「**驅動卡片（共鳴之錨）**」。SPEC 內 Scope、C-3 標題兩處已同步更新。
- **A-2、A-3 合併（原「履歷上傳冷啟動」＋「快速匯入冷啟動」）**：使用者要求把兩個獨立的冷啟動入口合併成一個。合併後定案：新 **A-2　履歷與快速匯入冷啟動（主＋客觀）**，UI上仍是主輸入框下方快捷選項清單裡的**一個**選項（不是兩個），點選後提供兩條子路徑——①上傳履歷／手動填寫（只產生客觀事件）、②貼上多行舊資料快速匯入（逐行判斷主客觀）。兩條子路徑各自的既有規則（PDF/DOCX解析、至少2段經歷門檻／換行分行解析、行末專案標籤語法等）完全沿用，只是收斂成同一個入口點與同一份UI，不需要使用者在兩個平行選項間先選一個。
  - 連帶效應：A組冷啟動入口從「三種」變成「**兩種**」（A-1、A-2），全文所有「三種冷啟動入口」「A-1／A-2／A-3」的措辭已改為「兩種冷啟動入口」「A-1／A-2」。
  - 連帶效應：整組重新編號——原 A-4語音輸入→**A-3**、原 A-5多輪對話→**A-4**、原 A-6產生洞察按鈕→**A-5**。全文交叉引用（Scope、Figma/Wireframe、A組內部、B-1～B-5）已同步更新。
  - SPEC 功能數量由 22 個降為 **21 個**（少一個編號，因為是合併不是刪除，兩個功能的規則內容都保留，只是併入同一個功能節點下）。
  - 🟡 本次合併未重新討論「選填不強制」「UI強烈引導完成履歷上傳子路徑」等既有規則是否需要因為介面整合而調整（例如：兩條子路徑合併成一個選項後，要怎麼在同一個入口UI裡凸顯「履歷上傳」比「快速匯入」更被鼓勵），沿用原規則文字，實際UI設計待team對照Figma確認。
- **新增「登入（簡易命名對應）」，比側邊欄更前置**：使用者要求在側邊欄之前，補一個最前置的簡易登入機制——使用者輸入名字，系統直接把名字對應到一組使用者ID，不做密碼／Email／第三方驗證。已寫入 SPEC「Function Detail」開頭、side bar 之前。
  - 🟡 這不是單純的規格空白，而是一個功能性風險：名字不具唯一性，「名字→使用者ID」的對應規則完全沒定義（同名字撞名怎麼辦、換裝置後能否用同一個名字找回原本資料）。已在 SPEC 內用 🟡 標記為「工程實作前必須先釐清」等級的開放問題，不是普通的UI文案問題，見下方開放事項。
- **A-4 System Prompt 草稿補充**：夥伴提供一份艾可輸出回應的 System Prompt 草稿作為工程實作參考，核心精神是「使用者感覺在跟教練聊天，不是被系統分類填表」，系統內部判斷邏輯（事件具體度、情緒明確度、矩陣、規則等術語）不可出現在艾可對使用者說的話裡。已原文寫入 SPEC A-4「AI教練人設與三段式回應結構」小節下方，標記為 🟡 待確認（草稿狀態，非定案）。
- **A-5〈產生洞察〉按鈕機制補充**：夥伴補充按鈕本質是一組特殊指令——點擊後系統代替使用者送出「Please Generate my insight」文字，等同使用者自己輸入，觸發艾可依LLM理解產出Echo Card。已寫入 SPEC A-5，並指出這與 A-4 單一事件深挖模式裡使用者可手動輸入同一句話跳過剩餘輪次是同一套機制，非兩套獨立邏輯。新增開放問題：使用者自己手打出一樣的句子算不算數，見下方開放事項。
- **A-4 補充「Echo Insights LLM 判斷邏輯」**：夥伴補充深挖模式底層的追問判斷邏輯——LLM對照使用者輸入與B-2 Echo Card六欄位模板逐項比對，盤點還缺哪些欄位，每次至多追問3題、挑最重要的問。已寫入 SPEC A-4「單一事件深挖模式」小節。放在 A-4 而非 A-5 的判斷依據：這是**產卡前**、對話進行中持續判斷要不要追問的邏輯，A-5 按鈕觸發時對話已經結束、直接生成卡片不再提問，兩者是時間序上不同階段。新增開放問題：這條「至多三題」規則跟 A-4 既有「每輪僅問一題」的規則衝突，尚未協調，見下方開放事項。
- **A-5 補充「Echo Card 各欄位 LLM 生成規則」**：夥伴提供 Figma 截圖，逐欄定義 B-2 六欄位各自怎麼由 LLM 生成（事件的發生／我的情緒：各自最多三個bullet point、每點≤50字；我在意/適合/擅長、我討厭/不適合/不在意：各生成一句動機價值基礎描述，且重申兩欄不可互為負命題的既有規則；我的價值主張：總結近一個月對話模式＋逐字引用佐證）。已寫入 SPEC A-5「Echo Card 各欄位 LLM 生成規則」小節。
  - 🟡 **重大發現：「和過去的我是否匹配」欄位的視覺呈現，Figma畫面與B-2現行文字定義不一致**。B-2現行定義是「0-100分＋文字判讀＋符合/落差關鍵字對照」；Figma畫面顯示的卻是雙色圓環圖（金色＝跟過去相似比例／核心特質，綠色＝新發現比例），且標註「規則：待定」。兩種定義先並存記錄，不擅自二選一，待team對齊，見下方開放事項。
  - 🟡 Figma畫面備註「洞察卡片其實有效能問題」，具體原因未說明，已如實記錄，見下方開放事項。
- **C 組（Dashboard）大量補充，夥伴提供 Figma wireframe 逐卡定義**：
  - **C-3 驅動卡片（共鳴之錨）改版**：主視覺從「三張卡片直向堆疊」改為**三圈交集Venn圖**（我擅長什麼／我想要什麼／我的標準是什麼），交集處置中顯示與C-7共用的主要職涯錨點類型結果；原本三張卡片內容保留，改為點擊〈查看完整說明〉才展開的詳細檢視。同時補上「共鳴之錨」命名由來：呼應 Edgar H. Schein 的 career anchor（生涯之錨）概念。
  - **C-4 文字雲補充兩種候選收集法**：①離散情緒標記（LLM每事件最多標5個情緒，累計計數）②LLM自定義關鍵字（不限情緒，每事件標5個關鍵字，累計計數）。尚未二選一，且點出這兩種法都涉及LLM判斷，可能跟C-4「純規則型統計」的既有定位互相矛盾，需team一併釐清。
  - **C-5 行為模式排行補充收集法**：從客觀事件萃取行為導向並歸納，取前3名。
  - **C-2 Persona卡片補充引言句生成規則**：LLM生成「使用者動機價值基礎＋職稱」組合句（例如「需要看到明確挑戰與實際影響的產品經理」）。
  - **C-7／C-8／C-9 補上完整技術方案**：三者是同一套框架的三層次——C-7個人特質本身、C-8特質對應職業環境（個人對個人）、C-9特質對應人際互動（個人對他人）。三者共用「逐事件記分、累加平均」的計算邏輯，差異只在面向數量（8/6/4）與最終視覺。C-9並補上完整的DISC座標轉換公式（`X=(Total_I+Total_S)-(Total_D+Total_C)`，`Y=(Total_D+Total_I)-(Total_S+Total_C)`）與四象限軸定義（X軸：關注事Task vs. 關注人People；Y軸：主動Active vs. 被動Reflective）。這批技術方案**直接回應了上方🚩第1、2、3項team分歧**——不是團隊已經拍板，而是把「LLM推論」這一側的方案細節補齊，供team開會時參考比較，SPEC內已用🟡標示非定案，見上方🚩分歧項目的更新說明。

### 260901　Scope 大幅縮範圍，改為明列 MVP 定義範圍（Webber）

- 使用者要求 Scope 從「列出A/B/C全部21個功能＋詳細說明」改成「只寫MVP定義的範圍」，大幅縮小本輪實際要做的功能清單。
- **Part 1 新範圍**：登入、側邊欄、A-1／A-2／A-4／A-5（**排除A-3語音輸入**）、B-2事件洞察卡片、C-2／C-3／C-4／C-5／C-7／C-10。
- **明確排除、移入Part 2**：A-3語音輸入；B-1時間軸呈現（完整視覺化）、B-3整體洞察、B-4跳轉對話按鈕、B-5篩選器、B-6情緒曲線；C-1版面配置、C-6軟硬實力證據、C-8 RIASEC長條圖、C-9 DISC象限圖。
- **使用者明確指出關鍵項目**：A-4、A-5、C-10 三項各自的規則定義（尤其是System Prompt／LLM判斷邏輯）是本輪MVP能否成立的關鍵，已在SPEC標🚩優先度最高。
- Scope條列格式同步簡化：拿掉逐項的長篇說明文字，只留功能名＋「詳見X-Y」指引，避免與各功能自己章節的內容重複。
- 🟡 **本次縮範圍未把 B-1（時間軸呈現）明列進 Part 1，但這造成潛在矛盾**：側邊欄 My Trail、B-2 錨點機制、C-2 代表事件標籤，三處既有規格都寫明依賴事件被記錄與定位到時間軸上的能力，若 B-1 完全不在本輪範圍內，這幾處規則需要重新檢視是否還能運作。SPEC內已加註提醒「MVP至少需要B-1的最小記錄機制，不一定要完整視覺化」，但這只是暫時的緩解說法，不是team已經拍板的解法，需要team盡快確認，見下方開放事項。

### 260906　Figma / Wireframe 清單精簡（Webber）

- 使用者要求 Figma / Wireframe 清單只留三項：Prototype（可互動版）、Pitch Deck、競品參考（tryapt.ai）。
- **移除項目**：Prototype v3／v4（HTML原型連結）、第三小組畫面流程圖（`曼陀號_第三小組.pdf`）、Figma流程圖（260830補充，含A-6兩按鈕序列與點數機制的發現說明）、Prototype完整腳本規格提示詞。這些連結本身不再列出，但它們當初補充進SPEC的內容（例如點數／額度機制的發現）已經寫進對應功能章節（A-5）與DECISIONS，不會因為這裡精簡而遺失。
- 精簡理由：Figma/Wireframe清單原本累積了很多一次性的補充來源連結，讀起來雜；只留下持續會用到、值得反覆點閱的三個核心參考連結。

### 260906　開放問題清單歸零重來（Webber）

- 使用者認為〈二、目前未決事項〉累積到24項後太雜、不好維護，決定清空重新開始追蹤，不逐項清理。
- 原本的24項內容**沒有刪除**，完整封存於 [archive/EchoTrail-DECISIONS-開放問題_260906前封存.md](archive/EchoTrail-DECISIONS-開放問題_260906前封存.md)，供追溯查閱。
- 🚩 提醒：封存不代表已解決。封存清單裡至少幾項是實質未解、優先度不低的（例如 C-7/C-8/C-9 分數資料來源的team分歧、B-1未列入Part1但側邊欄/B-2/C-2都依賴它、A-4「一次問3題」與既有規則衝突），建議team找時間過一輪封存清單，把還有效的項目手動搬回本節，不要單純視為歸零就沒事。
- 本文件〈一、決策紀錄〉不受影響，本次只清空〈二、開放問題〉。

### 260908　A-4 多輪對話大改（品質 gate／輪數上限／必填欄位）＋ C-8／C-9 回歸 MVP ＋ SPEC 標題層級重整（Webber）

本次對話逐項討論 A-4，改動橫跨規則本質、必填欄位定義與整段結構；同時反轉 260901 把 C-8／C-9 移出 MVP 的決定。拍板者：Webber（逐項討論）。

**1. 核心規則 1「品質 gate」澄清——MVP 不在任何階段設品質關卡**

- 原 260827 拍板「必要欄位門檻：有值就過關，品質交給 B-2 的信心分數／匹配分數機制把關」。本次逐一檢視 B-2 那三個分數，發現**沒有一個實際在把關品質**：內部信心分數對使用者隱藏、只做團隊排序與模型校準；匹配分數比的是「與過去的自己一致性」，不是「這次輸入寫得夠不夠具體」；使用者契合度是事後評分。B-2 唯一的硬規則是「客觀證據＋主觀證據缺一不可」，那是「有沒有」的結構檢查，不判斷空泛與否。
- 因此把 A-4 規則 1 改寫為：**本輪 MVP 明確不在任何階段對輸入品質設關卡**——A-4 不為品質卡使用者，B-2 也沒有機制會擋下／退回／要求補寫證據太薄的卡片。空泛輸入靠艾可三段式追問自然引導，引導不出來就照收。
- 選項比較：曾考慮 (2) 讓信心分數低於門檻時對使用者顯示提示、接回 B-4 跳轉對話；(3) A-4 端對只抓到程度副詞的情緒欄位再追問一次。都因為「跟輕量 AI prompt 的整體基調衝突、為 MVP 硬加機制」被否決，選最誠實的「就是不把關」。

**2. 輪數上限：軟＋硬雙層 → 單層，到頂只跳 CTA、永不強制結束對話**

- 推翻 260827 拍板的「軟上限（文案預期）＋硬上限（超過強制結束並標記資訊不完整）混合」。
- 新規則：**單一輪數上限 8 輪**。必要欄位收齊、或聊到 8 輪未收齊，系統做的事都只是「把 CTA 跳出來」，**永遠不主動結束對話**，使用者可無視 CTA 繼續聊。是否收斂一律由使用者點不點 CTA 決定。
- 「資訊不完整」標記改綁「使用者在必填欄位未收齊時就點 CTA 產卡」，與聊到第幾輪脫鉤。
- 3 輪（深挖模式邀請 CTA）／8 輪（批次模式輪數上限）確立為 **MVP 上線值**，拿掉原本「示意值、待實測校準」的模糊措辭，上線後可依實測調整、屬營運參數。

**3. Rephrase／Skip 降為 Nice to Have**

- 「每題可 Rephrase／Skip」從既有規則降為 **Nice to Have（非本輪 MVP 必做）**，視開發時程決定是否納入；未實作時使用者仍可直接在輸入框打字帶過。

**4. 必填欄位用「從 output 反推」重新定義：3 → 4，時間降級、動機價值語料升級**

- 方法：逐一清點每個 MVP output（B-2 六欄 ＋ C-2／C-3／C-4／C-5／C-7／C-8／C-9）的 LLM 生成規則需要什麼輸入，反推出「缺了下游產不出、或只能造假」的欄位＝必填。
- 原必填 3 欄：事件內容、時間、情緒用字。
- **新必填 4 欄**：① 事件內容描述　② 感受／情緒用字　③ 在意／重視的點（動機價值・正向）　④ 不能接受／不適合的點（動機價值・反向）。
- **時間降為非必填**：清點後沒有任何 MVP output 需要事件發生時間——C 組測評都是「逐事件記分、累加平均」與順序無關，B-1 完整時間軸在 Part 2，B-2 卡片上的日期標籤是裝飾。時間抓得到就記、抓不到標「現在（未確認時間）」，不影響 CTA 收斂。
- **③④（動機價值語料）從「非必填、靠 LLM 從對話推」升為必填**：理由是 C-2 引言句（動機價值基礎＋職稱）、C-3 動機基礎卡／價值基礎卡、C-7／C-8 的「想要／不妥協」維度都直接需要這兩類語料，缺了這些 🚩 關鍵功能早期產出等於 LLM 亂猜。曾考慮 (A) 維持 2 必填靠累積攤平、(C) 「必問但不擋」中間層，最後選「升為必填」。
- ③④ 一樣套「同一欄追問滿 2 次沒有 → 標『未確認』放行」，永遠不會死鎖。
- 新增兩個非必填欄位：情境細節（對象／場合／前因）、這件事對你的意義（價值主張語料）。
- **「算有值」判準改綁下游可用性**（回應「4 個必填 context 夠不夠 AI 用」的疑慮）：「有值」＝「下游 LLM 能不能用這段輸入產出對應的 Echo Card 欄位／分數」，不是主觀品質評分——極空泛回答（「就那樣」「不知道」）不算有值，但有可用訊號就過關。①要能撐起至少 1 條客觀 bullet、②要有可逐字引用的情緒字、③④要講出至少一個具體的在意／不能接受點。仍不設 gate、追問滿 2 次照樣放行。
- **新增規則 5「蒐集深度指引（非 gating）」**：必填「有值」只是 CTA 最低門檻，不是蒐集目標；CTA 跳出後艾可應繼續把各欄往「足以撐 2–3 條 bullet」的深度追，寫進艾可 prompt 當目標，但追不到不擋收斂、不影響「資訊不完整」判定。
- 連帶：CTA 收斂條件從「三欄」改「四個必填欄位」；反問順序改「事件內容 → 情緒用字 → 在意 → 不能接受」，必填收齊後艾可才追非必填（時間 → 專案標籤 → 意義語料）；範例對話追蹤改成 3 輪收齊 ①②③④ 才跳 CTA。

**5. A-4 整段結構重整（只去重，不改規則）**

- 從 11 個區塊、兩套平行敘述（「核心規則／反問決策規則／三段式」條列版 ＋「對話怎麼被系統追蹤」表格白話版，各自維護、改規則要兩邊同步）→ **6 個編號小節**：一、欄位定義　二、對話流程規則　三、艾可人設與回應結構　四、批次模式與深挖模式　五、範例對話追蹤　六、驗證用途。
- 新增「下游 output ↔ 需要的欄位對照」表，當「哪個欄位服務哪個下游」的**單一真實來源**，欄位表不再重複列下游，之後改規格不用兩邊同步。

**6. C-8 RIASEC 長條圖、C-9 DISC 象限圖 重新納入 MVP Part 1**

- 推翻 260901「明確排除、移入 Part 2」。
- 連帶：SPEC Scope Part 1 的 C 組加入 C-8／C-9、Part 2 移除；A-4 output 對照表加 4 列（C-8 六面向分數／解讀、C-9 四屬性分數／風格描述）。
- **不新增必填欄位**：C-8 靠 ①③（④加分）、C-9 靠 ①，都是既有必填。

**7. SPEC 標題層級明確化**

- 起因：Webber 反映 SPEC「亂亂的、層級不夠明確」。原本 A-1～C-10 全部平在 `###`，A／B／C 群組與登入／側邊欄只是粗體段落，A-4 內部六節也是粗體。
- 新層級：`## ` 大區塊 → `### ` 功能群組（全域／A／B／C）→ `#### ` 單一功能（含登入、側邊欄）→ `##### ` 複雜功能內部小節（目前只有 A-4）。功能卡片內的 User Story／功能概述／驗收條件／規則段落維持粗體。
- 功能標題加 MVP 範圍標記：Part 1 不標，Part 2 加 ` · Part 2`（A-3、B-1、B-3～B-6、C-1、C-6 共 8 項）。
- 連帶：側邊欄補一條驗收條件（原本沒有）；CLAUDE.md〈编修慣例〉的完整性檢查由數 `### ` 改為數 `#### `／`^☑️ 驗收條件`（各 23，一致）。
- 純結構調整，不動任何規格內容。

**連帶效應與待處理**（詳見〈二〉新增條目）

- A-2 快速匯入的事件不走 A-4 對話、不會有 ③④，餵 C-7／C-8／C-9 時偏薄——A-2 既有設計，非本次改出，但 C-8／C-9 納入後影響面變大。
- C-1 版面配置仍在 Part 2，但儀表板現在要排 C-2／C-3／C-4／C-5 ＋ 三項測評 C-7／C-8／C-9，排版壓力更大。
- 🟡「C-7／C-8／C-9 分數來源：LLM 逐事件推論 vs Webber 主張的標準化量表」的 team 分歧，原本只卡 C-7，現在三項 MVP 功能全依賴它，優先度提高。

---

## 二、目前未決事項（開放問題彙總）

_260906 歸零重新開始，舊清單見上方 260906 條目提到的封存檔。_

### 🚩 team 內部有分歧，需要開會拍板（優先）

1. **C-7／C-8／C-9 分數來源：LLM 逐事件推論 vs 標準化量表**（260908 升級，原封存清單項目）。260901 補充的技術方案是「每討論完一則事件，LLM 針對各面向給 1–10 分、累加平均」；Webber 主張「使用者實際填答標準化量表」。260908 把 C-8／C-9 納入 MVP 後，C-7／C-8／C-9 三項 MVP 功能全依賴這題，必須盡快拍板。SPEC 內三處 🟡 標示非定案。

### 待技術評估／實測

1. **A-4 輪數上限 3／8 的實際值**：260908 確立 3（深挖模式邀請 CTA）／8（批次模式輪數上限）為 MVP 上線值，上線後依實測對話長度分佈校準。

### 待補充規格細節

1. **C-1 版面配置在 Part 2，但 C-8／C-9 已納入 MVP**：儀表板現在要排 C-2／C-3／C-4／C-5 ＋ 三項測評 C-7／C-8／C-9，需要一個最小排版定義才能實作，不必等 C-1 完整規格。（與既有的「C-1 缺席」矛盾同源，範圍擴大）
2. **A-2 快速匯入的事件缺動機價值語料（③④）**：匯入事件不走 A-4 對話，只有客觀內容或逐行分類，沒有 ③「在意」④「不能接受」，餵 C-2／C-3／C-7／C-8 時偏薄。是否需要補救（例如匯入後補一輪對話補齊、或標記這批事件不計入測評分數），待 team 確認。
3. **A-4「深挖模式一次至多 3 題」與「每輪僅問一題」衝突**（原封存清單項目，SPEC 內仍以 🟡 並存記錄）：待 team 確認是只適用深挖模式，還是整組規則改成可一次問多題。

### 待 team 確認是否有必要處理（非阻塞）

_目前無條目。_
